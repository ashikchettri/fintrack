# frontend

React 19 + Vite + TypeScript SPA. Created in phase 3 (see `docs/ROADMAP.md`) with:

```bash
npm create vite@latest . -- --template react-ts
```

Empty until then — auth and finance APIs come first.
