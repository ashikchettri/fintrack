# Trivy vulnerability allowlist. Keep this SHORT and time-boxed: each entry is a
# known HIGH/CRITICAL we've consciously accepted, with a reason and an exit plan.
#
# Currently empty — pgjdbc CVE-2026-54291 was cleared by pinning postgresql to
# 42.7.13 (see gradle/libs.versions.toml). Add new exceptions here only with a
# reason and a removal condition.
