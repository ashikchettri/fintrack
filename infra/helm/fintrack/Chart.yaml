apiVersion: v2
name: fintrack
description: FinTrack — household finance platform (auth, finance, gateway, insight) with Postgres, Redis and Mailpit
type: application
# Chart version — bump on chart changes. appVersion tracks the service images.
version: 0.1.0
appVersion: "0.0.1-SNAPSHOT"
