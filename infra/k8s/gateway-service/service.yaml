apiVersion: v1
kind: Service
metadata:
  name: gateway-service
spec:
  # The single public entry point (ADR 007). An Ingress will front this next.
  selector:
    app.kubernetes.io/name: gateway-service
  ports:
    - name: http
      port: 8080
      targetPort: http
